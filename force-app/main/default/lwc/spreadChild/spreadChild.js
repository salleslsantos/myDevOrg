import { api, track, LightningElement } from 'lwc';

export default class spreadChild extends LightningElement {
    @api playerposition;
    @api playername;
    @api playerPropTwo;
    @api nome;
    @api posicao;
}